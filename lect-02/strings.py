# Strings

hi = 'Hello there'  # ' ' single quotes
greetings = "Hello" # " " double quotes

fo = "this isn't right"

name = "Jose"

# Concatenate
greet = greetings + " " + name # Hello Jose

# Operations

3 * 'hi'  # Suce
len(name) # length of a string
name[0]   # Returns the first element
name[len(name) - 1] # Returns the last element
name[1:3] # Slicing a string
name[:3]
name[1:]
name[:] # Copy the entire string

